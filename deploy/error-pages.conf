# Included in the HTTPS server for open-aec.com and www.open-aec.com.
# Serves the site's own 404 page instead of nginx's default one. The page
# lives at the web root (404.html) and uses absolute paths only, so it renders
# correctly at any URL.
error_page 404 /404.html;
